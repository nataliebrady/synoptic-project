<% provide(:title, 'All users') %>

<div class="row mainpage-container">
 
<h1>All users</h1>

<div class="col-md-12">
  
<%= will_paginate %>
  
<ul class="users">
  <% @users.each do |user| %>
      <%= render user %>
  <% end %>
</ul>

<%= will_paginate %>
  
</div>
</div>